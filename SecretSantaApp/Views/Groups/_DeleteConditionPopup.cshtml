@model MemberConditionsEditModel
@{
    var uid = Guid.NewGuid();
    var containerid = $"divcontainerid-{uid}";
}

<div id="@containerid">
    @if (Model.Saved)
    {
        <p>you created the rule, we need to refresh the </p>

        <script>
            ClosePopup();
            LoadRules();
        </script>
    }
    else
    {
        <div class="container col-lg-12 ControlBox" style="background-color: red">
            <div class="">
                <div class="" style="display: inline !important">
                    <span>
                        <img src="~/images/smSantaMeanMug.png" alt="Reindeer" class="img-responsive" style="display: inline !important">
                    </span>

                    @if (Model.ConditionId >= 1)
                    {
                        <h3 class="text-center" style="color: white; display: inline !important">Delete Condition? @Model.ConditionId</h3>
                    }

                    <span>
                        <img src="~/images/smSantaMeanMug.png" alt="Reindeer" class="img-responsive" style="display: inline !important">
                    </span>
                </div>
                <div class="row col-xs-12 center-block RoundCornerBox" style="background-color: lightgray; padding-bottom: 2%;">

                    <form asp-action="DeleteCondition" method="POST"
                          data-ajax="true"
                          data-ajax-method="POST"
                          data-ajax-mode="replace"
                          data-ajax-update="#@containerid">


                        <div class="form-group">
                            <div class="col-sm-offset-2 col-sm-8">
                                <input type="submit" name="signin" id="signin" value="Delete" class="btn btn-default  BtnRed"/>
                                <button id="" type="button" class="btn btn-default BtnGreen" onclick="ClosePopup()">Cancel</button>
                            </div>
                        </div>
                        @Html.HiddenFor(x => x.ConditionId)
                        @Html.HiddenFor(x => x.GroupId)
                        @Html.HiddenFor(x => x.InsertedBy)
                    </form>
                </div>
            </div>

        </div>
        <!-- /.container -->

        <script>

        </script>
    }


</div>